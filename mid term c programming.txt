1) factorial program 


--> 



#include<stdio.h>
int main()
{
int fact_start,i,factorial=1;
printf("enter the factorial number : ");
scanf("%d",&fact_start);
for(i=fact_start;i>0;i--)
{
factorial*=i;
}
printf("the factorial is : %d ", factorial);
}
    





2) gcd or hcf program 


--> 


#include<stdio.h>
int main()
{
    int a,b,max,hcf,i;
    printf("enter 1st number : ");
    scanf("%d",&a);
    printf("enter 2nd number : ");
    scanf("%d",&b);

    for (i=1;i<=a&i<=b;i++)
    {
        if (a%i==0 & b%i==0)
        {
            hcf = i;
        }
    }
printf("hcf is : %d ",hcf);
}







3) hanoi program




#include<stdio.h>
#include<stdlib.h>
void hanoi(int n,char start,char middle,char end)
{
   if (n==1)
   {
       printf("move %d from tower %c to tower %c \n",n,start,end);
       return;
   }
       hanoi(n-1,start,end,middle);
       
       printf("move %d from tower %c to tower %c \n",n,start,end);
       hanoi(n-1,middle,start,end);
}

int main()
{
int n;
printf("enter the number of disc : ");
scanf("%d",&n);
hanoi(n,'A','B','C');
return 0;
}







4) finding min max





#include<stdio.h>
int main()
{
int i,n,max,min,arr[100];
printf("enter the number of elements you need from (1 to 100) : ");
scanf("%d",&n);
for(i=0;i<n;i++)
{
printf("enter the element %d : ",i+1);
scanf("%d",&arr[i]);
}
max=arr[0];
min=arr[0];
for(i=1;i<n;i++)
{
if(arr[i]>max)
max=arr[i];
else if(arr[i]<min)
min=arr[i];
}
printf("the max element = %d \t the min element = %d",max,min);
}






5)sequential search




#include<stdio.h>
int main()
{
int number,i,search,arr[100];
printf("enter the number from (1 to 100)");
scanf("%d",&number);
for(i=0;i<number;i++)
{
printf("enter the %d element",i+1);
scanf("%d",&arr[i]);
}
printf("enter the number to search : ");
scanf("%d",&search);
for(i=1;i<number;i++)
{
if(arr[i]==search)
printf("the number %d is present in location %d",number,i+1);
break;
}
if(i==number)
printf("the number is not present in loop");
}







6) bubble sort





#include<stdio.h>
int main()
{
int array[100], n, i, j, temp;
printf("Enter number of elements\n");
scanf("%d", &n);
printf("Enter %d Numbers:\n", n);
for(i = 0; i < n; i++)
scanf("%d", &array[i]);
for(i = 0 ; i < n - 1; i++)
{
for(j = 0 ; j < n-i-1; j++)
{
if(array[j] > array[j+1])
{
temp=array[j];
array[j]=array[j+1];
array[j+1]=temp;
}
}
}
printf("Sorted Array:\n");
for(i = 0; i < n; i++)
printf("%d\t", array[i]);
return 0;
}








7) factorial recersive







#include <stdio.h>
int factorial(int);
int main()
{
int num,result;
printf("Enter a number to find it's Factorial: ");
scanf("%d", &num);
if (num < 0)
{
printf("Factorial of negative number not possible\n");
}
else
{
result = factorial(num);
printf("The Factorial of %d is %d.\n", num, result);
}
return 0;
}
int factorial(int num)
{
if (num == 0 || num == 1)
{
return 1;
}
else
{
return(num * factorial(num - 1));
}
}

